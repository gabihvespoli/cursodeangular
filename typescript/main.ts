let minhaVar = 'var feliz'

function minhaFunc(x, y) {
  return x + y
}
let numeros = [1, 2, 3]

numeros.map(valor => valor * 2)

class Matematica {
  soma (x, y){
    return x + y
  }
}

var n1 = 'ssdf'